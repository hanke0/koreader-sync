It's a Go version of (koreader-sync-server)[https://github.com/koreader/koreader-sync-server].

# How to use.
Download the binary at release page and setup by command:
```
koreader-sync -addr 127.0.0.1:9200 -db koreader.sqlite
```
